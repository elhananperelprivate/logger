import { Route } from '@angular/router';
import { SimulateErrorComponent } from '../components/simulate-error/simulate-error.component';

export const remoteRoutes: Route[] = [
  { path: '', component: SimulateErrorComponent },
];
